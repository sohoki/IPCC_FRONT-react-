# 2026-06-17 작업 로그

## 13:27 — IVR 모달 한글 복원 및 IvrFormModal 재구성

**파일:**
- `src/pages/Backoffice/Infra/call/ivr/components/IvrCallbackModal.jsx`
- `src/pages/Backoffice/Infra/call/ivr/components/IvrHolyModal.jsx`
- `src/pages/Backoffice/Infra/call/ivr/components/IvrWorkModal.jsx`
- `src/pages/Backoffice/Infra/call/ivr/components/IvrFormModal.jsx` (재구성)
- `src/pages/Backoffice/Infra/call/ivr/IvrConfigInfo.jsx`

### 변경 내용
- EUC-KR → UTF-8 변환 실패로 JSX 구조까지 손상된 IVR 모달 3개 한글 복원
  - 따옴표·꺾쇠(`<`) 등 JSX 구조 문자가 CP949 2바이트 시퀀스에 흡수되어 파일 손상
  - 코드 로직은 유지하고 한글 문자열만 올바르게 재작성
- `IvrFormModal.jsx` — 이전 세션에서 0바이트로 삭제됨 → lazy import 실패 → `Cannot convert object to primitive value` 오류 발생. 컬럼 정의·URL 상수 기반으로 재구성
- `IvrConfigInfo.jsx` — `formKey`·`callbackKey` 카운터 추가 → 모달 오픈 시 `key` 변경으로 컴포넌트 리마운트, 폼 state 자동 초기화 (useEffect 안의 직접 setState 제거)
- `IvrCallbackModal.jsx` — useEffect 제거, `useState` lazy initializer로 폼 초기화
- `IvrHolyModal.jsx` / `IvrWorkModal.jsx` — 최초 조회를 `fnAjaxFetch(...).then()` 직접 호출로 변경

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `IvrFormModal`의 `IVR_INSTT_COMBO` API 파라미터 형태를 확인 필요 (응답 구조에 따라 `o.insttCode`/`o.code` 분기 처리)
- 원본 IvrFormModal 로직(중복확인 등) 불명확 — 백엔드 확인 후 보완 필요

---

## 13:40 — ServerInfo 상태 표시 및 연동방법 버튼화

**파일:** `src/pages/Backoffice/Infra/Equipment/ServerInfo.jsx`

### 변경 내용
- `상태` 컬럼 (`serverStatus`) — 원형 LED 인디케이터 + 텍스트로 변경
  - `fail` → 빨간불(#dc3545) + "에러"
  - 정상값 → 파란불(#0d6efd) + "정상"
  - 미조회 → 회색(#adb5bd) + "미확인"
- `연동방법` 컬럼 (`serverMethodTxt`) — `btn-link` → `btn-outline-secondary btn-sm` 버튼으로 변경

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `serverStatus` 값이 정확히 `"fail"` (소문자) 인지 백엔드 확인 필요. 대소문자 무관 처리(`toLowerCase()`)로 대응

---

## 15:10 — ServerInfo 상태 상세 모달 및 SystemService 모달 인코딩 복원

**파일:**
- `src/pages/Backoffice/Infra/Equipment/components/ServerStatusModal.jsx` (신규)
- `src/pages/Backoffice/Infra/Equipment/ServerInfo.jsx`
- `src/pages/Backoffice/Infra/Equipment/components/SystemServiceFormModal.jsx`
- `src/pages/Backoffice/Infra/Equipment/components/SystemServiceOidFormModal.jsx`
- `src/pages/Backoffice/Infra/Equipment/SystemServiceInfo.jsx`
- `src/constants/URL.jsx`
- `backend/.../sym/svr/service/ServerInfoManageService.java`
- `backend/.../sym/svr/web/ServerInfoManageController.java`

### 변경 내용
- 백엔드 `GET /api/backoffice/sym/svr/status/{serverCode}.do` 엔드포인트 추가 (`TB_SERVER_STATUS` 최신 1건 조회)
- `ServerStatusModal.jsx` 신규 생성 — CPU/메모리/디스크 게이지 + PING 상태 표시
- `ServerInfo.jsx` — 상태 컬럼 클릭 시 모달 오픈 연결
- `SystemServiceFormModal.jsx` — EUC-KR 인코딩 손상으로 JSX 구조 파괴 (useEffect·함수 선언이 `//` 주석에 흡수) → 전체 재작성, `key` 리마운트 + lazy initializer 패턴 적용
- `SystemServiceOidFormModal.jsx` — 한글 깨짐 복원, useEffect 폼 초기화 제거, lazy initializer 패턴 적용
- `SystemServiceInfo.jsx` — `serviceFormKey`·`oidFormKey` 추가, 모달 오픈 4곳에 카운터 증가, `key` prop 전달

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 서버 상태 API 응답 경로가 `res.data.result.result` (이중 중첩) — 백엔드 ResultHelper 구조에 따른 것으로 정상

---

## 현재 시각 — SystemServiceFormModal SNMP 버전 선택 미표시 수정

**파일:** `src/pages/Backoffice/Infra/Equipment/components/SystemServiceFormModal.jsx`

### 변경 내용
- `useCommonCodeData('SNMP_VERSION')` 제거 → `SNMP_VERSION_OPTIONS` 상수로 교체
  - **원인:** `selectCmmnDetailComboLamp` MyBatis 쿼리가 `systemCode` 파라미터를 요구하지만 서비스가 단일 String만 전달해, Redis에 없는 코드 그룹은 DB fallback도 항상 빈 배열 반환
  - 결과: `snmpVersionOptions = []` → SNMP Version select 옵션 없음 → 사용자가 V3 선택 불가 → `showSnmpV3Fields = false` 유지 → V3 전용 입력 폼(PWD/Auth/Privacy) 미표시
- 하드코드 목록 `SNMP_VERSION_2` / `SNMP_VERSION_3` — 백엔드 `executeSnmpQuery` switch case 값과 일치

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- DB `COMTCCMMNCODE` + `COMTCCMMNDETAILCODE`에 `SNMP_VERSION` 그룹 + 세부코드를 등록하면 추후 공통코드로 전환 가능 (현재는 하드코드로 안정 운영)

---

## 16:59 — IVR 사용여부·멘트사용여부 UseSwitch 및 멘트 입력 모달

**파일:**
- `backend/.../ivr/diotis/service/IvrConfigInfoManageService.java`
- `backend/.../ivr/diotis/web/IvrConfigInfoManageController.java`
- `src/constants/URL.jsx`
- `src/pages/Backoffice/Infra/call/ivr/IvrConfigInfo.jsx`
- `src/pages/Backoffice/Infra/call/ivr/components/IvrFormModal.jsx`

### 변경 내용
- **백엔드 Service** — `updateIvrUseyn`, `updateIvrMentUseyn` 메소드 추가
- **백엔드 Controller** — `@PatchMapping("useyn/{ivrCode}.do")`, `@PatchMapping("mentUseyn/{ivrCode}.do")` 엔드포인트 추가
- **URL.jsx** — `IVR_USEYN`, `IVR_MENT_USEYN` 상수 추가
- **IvrConfigInfo.jsx**
  - `사용여부` 컬럼: 기존 broken UseSwitch → `IosSwitch` + `PATCH IVR_USEYN` 으로 교체
  - `멘트사용여부` 컬럼: 텍스트 → `IosSwitch` 추가
    - OFF(N): `PATCH IVR_MENT_USEYN` 직접 호출
    - ON(Y): 멘트 내용·시작일·종료일 입력 모달 표시 → IVR_UPDATE(full update) 호출
- **IvrFormModal.jsx**
  - `사용여부`·`멘트사용여부` select → `IosSwitch` 교체
  - `ivrMent` 필드 buildForm에 추가
  - 멘트사용여부 Y일 때만 멘트 내용(textarea)·시작일·종료일 표시
  - handleSave에 `ivrMent` 포함

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 그리드 멘트 ON 저장 시 `IVR_UPDATE` 전체 업데이트 사용 — `workStime/workEtime/ivrCreatefileCode` 등 rowData에서 가져오므로 기존 값 유지됨

---

## 17:10 — 서버 현황 모니터링 요약 카드 UI 추가

**파일:**
- `backend/.../sym/svr/mapper/ServerInfoManageMapper.xml` — `selectServerSummary` SQL 추가
- `backend/.../sym/svr/mapper/ServerInfoManageMapper.java` — mapper 메소드 추가
- `backend/.../sym/svr/service/ServerInfoManageService.java` — service 메소드 추가
- `backend/.../sym/svr/web/ServerInfoManageController.java` — `GET summary.do` 엔드포인트 추가
- `src/constants/URL.jsx` — `SERVER_SUMMARY` 상수 추가
- `src/pages/Backoffice/Infra/Equipment/ServerInfo.jsx` — 모니터링 카드 UI 추가

### 변경 내용
- `TB_SERVERINFO` 집계 쿼리: 전체/정상/에러/미확인/사용중/미사용 건수 반환
- `GET /api/backoffice/sym/svr/summary.do` 엔드포인트 신규
- 서버 현황 페이지 상단에 5개 모니터링 카드 추가
  - 전체 서버 (보라) / 정상 서버 (초록) / 에러 서버 (빨강) / 미확인 서버 (노랑) / 사용중 서버 (하늘)
  - 다크 그라디언트 카드 + 하단 색상 accent 바 디자인
  - 페이지 최초 진입 시 자동 조회

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음
