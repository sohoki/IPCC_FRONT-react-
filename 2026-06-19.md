# 2026-06-19 작업 로그

## 09:00 — AlertPartDetailRenderer 삭제 후 서브그리드 RELOAD 처리
**파일:** `frontend/src/pages/Backoffice/Manager/Message/components/AlertPartDetailRenderer.jsx`

### 변경 내용
- `useCommonDelete` 내부화 — 삭제 성공 시 `reloadFunction: loadData`로 자체 reload
- `loadData`를 `useCallback`으로 추출 (기존 `useEffect` 내 익명 fetch 로직)
- `columnDefs`를 `useMemo`로 컴포넌트 내부화 (`handleDelete` 참조 필요)
- `PART_COL_DEFS` 외부 상수 제거

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 기존 `context.onDeletePart` 경로는 `getDetailGridInfo`가 커스텀 렌더러에서 동작 안 해서 내부 처리로 전환

---

## 09:30 — AlertMessageInfo 서브그리드 등록 후 RELOAD 처리
**파일:** `frontend/src/pages/Backoffice/Manager/Message/AlertMessageInfo.jsx`
**파일:** `frontend/src/pages/Backoffice/Manager/Message/components/AlertPartDetailRenderer.jsx`

### 변경 내용
- `refreshCallbacks` useRef 도입 — 각 서브그리드의 `loadData`를 alertSeq 키로 등록
- `refreshPartForAlert` → `refreshCallbacks.current[alertSeq]?.()` 호출 방식으로 교체
- `gridContext`에 `registerRefresh` / `unregisterRefresh` 추가
- `AlertPartDetailRenderer`에 마운트/언마운트 시 context에 loadData 등록/해제 useEffect 추가
- `MasterDetailGrid`에 `detailCellRendererParams={{ context: gridContext }}` 추가
  - AG Grid는 `detailCellRenderer`에 그리드 `context`를 자동 전달하지 않음 → 명시적 전달 필요

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- AG Grid `detailCellRenderer`에서 `props.context`는 `detailCellRendererParams`로 명시 전달해야 함 (자동 상속 안 됨)

---

## 10:00 — AlertRecModal UI 개선 및 전화번호 검증
**파일:** `frontend/src/pages/Backoffice/Manager/Message/components/AlertRecModal.jsx`

### 변경 내용
- 등록 입력 행을 별도 div → 테이블 첫 번째 `tbody` 행으로 통합 (등록 버튼이 삭제 버튼 열에 정렬)
- 사용유무 라디오 버튼 → `IosSwitch`로 교체
- 전화번호 자동 포맷팅 (`formatPhone`) 및 유효성 검증 (`isValidPhone`) 추가
- React Compiler 대응: `useEffect` 내 setState를 `.then()` 콜백 패턴으로 처리
- 데이터 행 `color: '#fff'` 추가 — 다크 테마 모달에서 텍스트 안 보이는 문제 수정

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 없음

---

## 10:30 — 부서·담당자 중복 등록 차단 (백엔드)
**파일:** `backend/.../sym/alt/service/repository/AlertPartRepository.java`
**파일:** `backend/.../sym/alt/service/repository/AlertRecInfoRepository.java`
**파일:** `backend/.../sym/alt/service/AlertPartInfoManageService.java`
**파일:** `backend/.../sym/alt/service/AlertRecInfoManageService.java`
**파일:** `backend/.../sym/alt/web/AlertMessageManageController.java`

### 변경 내용
- `AlertPartRepository` — `existsByAlertSeqAndAlertInsttCodeAndAlertPartAndPartGubun` 추가
- `AlertRecInfoRepository` — `existsByAlertSeqAndRecTelNumber` 추가
- `AlertPartInfoManageService` — `isDuplicatePart()` 추가
- `AlertRecInfoManageService` — `isDuplicateRec()` 추가 (전화번호 하이픈 제거 후 비교)
- `AlertMessageManageController` — 두 등록 엔드포인트에 `mode=Ins` 시 중복 체크 → `resultCodeInfo=FAIL` + `resultMessage` 반환

### 중복 판단 기준
| 대상 | 키 조합 |
|---|---|
| 부서 | alertSeq + alertInsttCode + alertPart + partGubun |
| 담당자 | alertSeq + recTelNumber (하이픈 제거) |

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 수정 모드(`mode=Edt`)는 중복 체크 제외

---

## 15:00 — 내선번호 관리 페이지 React 변환 (PbxInfo.jsx)
**파일:** `frontend/src/pages/Backoffice/Infra/call/pbx/PbxInfo.jsx`
**파일:** `frontend/src/pages/Backoffice/Infra/call/pbx/components/PbxStationFormModal.jsx`
**파일:** `frontend/src/pages/Backoffice/Infra/call/pbx/components/PbxStationPbxSearchModal.jsx`
**파일:** `frontend/src/pages/Backoffice/Infra/call/pbx/components/PbxStationExcelModal.jsx`
**파일:** `frontend/src/constants/URL.jsx`

### 변경 내용
- 기존 jQuery/jqGrid HTML(`내선번호 관리`) → React 변환
- `URL.jsx`에 STATION_* 상수 8개 추가
- `PbxInfo.jsx` — `useGridInfinite` 기반 무한 스크롤 그리드, 검색·삭제 기능
- `PbxStationFormModal.jsx` — 등록/수정 팝업
  - 번호설정: 자동생성(기관 선택 + STATION_RANDOM) / 중복확인(STATION_CHECK) / Swal 방식 선택
  - TYPE·LOCATION·언어는 `useCommonCodeData` 코드 콤보
  - IP Softphone: IosSwitch (사용/사용 안함, y/n)
  - 버튼 설정 서브테이블: INDEX(1-100) + Data1~7, 행 클릭 시 수정 모드 전환
- `PbxStationPbxSearchModal.jsx` — notiSeq=101, qualifier `ext {from} count {n}` 조회, 체크박스 선택 후 일괄 등록
- `PbxStationExcelModal.jsx` — xlsx 파싱(Extension 컬럼) + 복사할 내선번호 입력 후 STATION_EXCEL_UPDATE 호출

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- IosSwitch onValue/offValue prop 지원 여부 백엔드 연동 시 확인 필요
