# 2026-06-15 작업 로그

## 15:08 — ManagerTapInfo.jsx 전면 재작성 + VendorDetailInfo.jsx 탭 연결

**파일:**
- `frontend/src/pages/Backoffice/Vendor/ManagerTapInfo.jsx`
- `frontend/src/pages/Backoffice/Vendor/VendorDetailInfo.jsx`

### 변경 내용
- `ManagerTapInfo.jsx` 구 패턴(AgGridReact 직접 사용, themeQuartz, IsoSwitch, 구형 훅 등) → 현재 프로젝트 표준 패턴으로 전면 재작성
  - `useGridInfinite`, `useCommonDelete`, `useCommonSubmit`, `useCommonCodeData`, `useCustomReqDataCombo`, `useIdCheck`, `useResetForm` 적용
  - `AppAgGrid` + `gridTheme` 사용
  - `CommonSelect`, `IosSwitch` 컴포넌트 사용
  - `checkField` 키를 `type`으로 통일 (구: `inputType`)
  - ID 중복체크 URL: `API_URL.VENDER_USER_ID_CHECK`
  - 비밀번호 초기화 URL: `API_URL.VENDER_USER_PWD_RESET`
  - 백엔드 컨트롤러 `VendorUserInfoManagerController.java` 기준으로 URL 매핑
- `VendorDetailInfo.jsx`에서 placeholder `ManageTabInfo` 제거, 실제 `ManagerTapInfo` import 및 연결

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `USER_STATE` 공통 코드가 DB에 등록되어 있어야 상태 콤보 정상 표시됨
- 권한 콤보는 `ROLE_GUBUN_2` 필터로 거래처 전용 권한만 조회
- 비밀번호 compareField는 항상 동작 (빈값 == 빈값 통과, 불일치 시 경고)

---

## 17:30 — VendorInfo.jsx 그리드 회사명 컬럼에 로고 표시 추가

**파일:** `frontend/src/pages/Backoffice/Vendor/VendorInfo.jsx`

### 변경 내용
- `comName` 컬럼에 `cellRenderer` 추가 — 회사명 앞에 로고 이미지 표시
- `comLogo` 값(파일명)이 있으면 `/upload/{comLogo}` URL로 이미지 로드
- 로고 없음 또는 이미지 로드 실패 시 SVG 기반 no-image 플레이스홀더 표시
- `NO_IMG_SRC` 상수: SVG를 data URI로 인코딩하여 오류 시 `onError`에서 src 교체
- 파일 서빙 경로 근거: `Globals.filePath = /usr/local/nginx/html/upload` (EgovConfigAppProperties.java 하드코딩), nginx가 `/upload/` 경로로 정적 서빙

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 개발 환경(nginx 미실행)에서 `/upload/` 경로 접근 불가 시 no-image 플레이스홀더가 자동 표시됨
- `onError`에서 src를 data URI로 교체하므로 무한 루프 없음 (data URI는 onError 미발생)

---

## 17:50 — VendorInfo.jsx 수정 버튼 수정 및 회사명 클릭 네비게이션 추가

**파일:** `frontend/src/pages/Backoffice/Vendor/VendorInfo.jsx`

### 변경 내용
- `openComModal` 수정: API 응답 `obj`가 null일 때 에러 다이얼로그 없이 조용히 실패하던 문제 → `else` 분기에 Swal 경고 추가
- `useNavigate` 추가 (`react-router-dom`)
- `handleNameClick` 추가: 회사명 클릭 시 `/sub/client/detail/${comCode}?comGubun=${comFirstGubun}` 로 이동
- `comName` cellRenderer 회사명 `<span>` 에 `cursor: pointer`, 파란색 underline 스타일 및 `onClick` 연결
- `columnDefs` useMemo deps에 `handleNameClick` 추가

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 수정 버튼 무반응 원인: API 성공했으나 `result.result`가 null인 경우 아무 피드백 없이 종료되던 로직 개선
- 네비게이션 경로: `/sub/client/detail/:comCode` (routes/index.jsx 기준)

---

## 18:30 — fileService NullPointerException 수정 (백엔드 컨트롤러 6개)

**파일:**
- `backend/.../infra/cor/com/web/CompanyInfoManageController.java`
- `backend/.../infra/bld/partclass/web/PartClassInfoManageController.java`
- `backend/.../infra/bld/floor/web/FloorInfoManageController.java`
- `backend/.../infra/bld/floor/web/FloorPartInfoManageController.java`
- `backend/.../infra/bld/cnt/web/CenterInfoManageController.java`
- `backend/.../infra/ble/wre/web/StorageInfoManageController.java`

### 변경 내용
- 원인: `fileService uploadFile = new fileService();` 로 직접 인스턴스 생성 → Spring DI 미작동 → `idgenService`(@Autowired) null
- 수정: `new fileService()` 제거, Spring 빈 주입 방식으로 변경
  - CompanyInfoManageController, StorageInfoManageController: `private final fileService uploadFile;` (@RequiredArgsConstructor)
  - 나머지 4개: `@Autowired private fileService uploadFile;` (기존 @Autowired 혼합 패턴 유지)

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 파일 업로드 기능이 있는 모든 컨트롤러에 동일 패턴이 있었으므로 일괄 수정
- `EgovConfigAppIdGen.java`의 `egovFileIdGnrService` 빈은 정상 등록되어 있었음 (문제는 주입이 안 된 것)
