# 2026-06-18 작업 로그

## 11:28 — IvrConfigInfoManageController 공용 IVR 전송 메서드 + 자동 전송 구현
**파일:** `E:\dev\front\egov\IPCC\backend\src\main\java\com\system\ipcc\ivr\diotis\web\IvrConfigInfoManageController.java`

### 변경 내용
- `sendServiceIni(String ivrCode, IvrConfigInfo rollbackConfig)` private 메서드 추가
  - DARS/Callback _service.ini 파일 생성 및 두 IVR 서버 SFTP 전송
  - SFTP 실패 시 rollbackConfig로 DB 원복 후 예외 throw
- `sendMainIni(String ivrCode)` private 메서드 추가
  - notice/WorkTime/WorkTimeList/Holiday 메인 .ini 생성 및 두 IVR 서버 SFTP 전송
  - 기존 selectIvrFileCreateInfoManage의 인라인 로직을 공용 메서드로 추출
- `updateSystemInfo` (ivrUpdate.do): DB 저장 → sendMainIni 자동 호출, 실패 시 이전 전체 정보로 DB 원복
- `updateIvrCallbackUpdate` (ivrCallbackUpdate.do): 인라인 SFTP 로직 → sendServiceIni 호출로 교체
- `updateIvrHoly` (updateIvrHoly.do): DB 저장 → sendMainIni 자동 호출 (원복 불가, 에러 메시지 반환)
- `updateIvrWorkTime` (updateIvrWorktime.do): DB 저장 → sendMainIni 자동 호출 (원복 불가, 에러 메시지 반환)
- `updateIvrUseyn` (useyn/{ivrCode}.do): DB 저장 → sendMainIni 자동 호출, 실패 시 이전 useyn으로 DB 원복
- `updateIvrMentUseyn` (mentUseyn/{ivrCode}.do): DB 저장 → sendMainIni 자동 호출, 실패 시 이전 멘트 정보로 DB 원복
- `selectIvrFileCreateInfoManage` (file/{ivrCode}.do): sendMainIni 호출로 간소화 (수동 전송용 API)

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- 휴일/업무시간 저장(updateIvrHoly, updateIvrWorkTime)의 경우 INSERT/UPDATE/DELETE가 혼재하여 DB 원복 로직 구현이 어려움 → 저장 완료 후 IVR 전송 실패 시 에러 메시지만 반환
- SFTP 포트: 기존 selectIvrFileCreateInfoManage에서 하드코딩(22) → sendMainIni에서 @Value ivrPort로 통일
