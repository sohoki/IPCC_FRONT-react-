# 2026-06-16 작업 로그

## 10:00 — useCommonDelete 훅 적용 (Queue/Exchange 삭제)

**파일:**
- `backend/.../sys/rabbitmq/web/AmqpConfigInfoManageController.java`
- `frontend/src/constants/URL.jsx`
- `frontend/src/pages/Backoffice/System/components/QueueTab.jsx`
- `frontend/src/pages/Backoffice/System/components/ExchangeTab.jsx`

### 변경 내용
- 백엔드: `@PathVariable` 방식 신규 엔드포인트 2개 추가
  - `DELETE queue/{processNm}.do` — Queue 단건 삭제
  - `DELETE exchange/{processNm}.do` — Exchange 단건 삭제
- `URL.jsx`: `QUEUE_DELETE` → `/api/.../amqp/queue`, `EXCHANGE_DELETE` → `/api/.../amqp/exchange` (`.do` 없는 base path)
- `QueueTab.jsx`: 커스텀 handleDelete 제거 → `useCommonDelete` 훅으로 교체, 셀 렌더러 `{ code, name }` 형식으로 변경
- `ExchangeTab.jsx`: 동일하게 `useCommonDelete` 훅 적용

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `useCommonDelete`는 `${URL}/${encodeURIComponent(code)}.do` 패턴으로 URL을 구성하므로 백엔드 엔드포인트가 path variable 방식이어야 함
- 기존 `deleteQueues.do` 엔드포인트는 그대로 유지 (Binding 등 다른 곳에서 사용 가능)

## 11:00 — Queue 유형(QUEUE_TYPE) 등록 반영

**파일:**
- `backend/.../sys/rabbitmq/web/AmqpConfigInfoManageController.java`
- `frontend/src/pages/Backoffice/System/components/QueueTab.jsx`
- `frontend/src/pages/Backoffice/System/components/QueueFormModal.jsx`

### 변경 내용
- `QueueFormModal.jsx`: Queue 유형 select 추가 (Classic / Quorum / Stream)
- `QueueTab.jsx`: INITIAL_FORM에 `queueType: 'classic'` 추가, 그리드에 'Queue 유형' 컬럼 추가 (`codeNm` 필드)
- 백엔드 `createQueues.do`: `queueType` 파라미터 읽어서 MQ 선언 시 `x-queue-type` 인수 전달 (classic은 인수 생략), DTO builder에 `.queueType(queueType)` 추가

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `QueueInfoManageMapper.xml` SELECT 쿼리가 `INNER JOIN COMTCCMMNDETAILCODE ON QUEUE_TYPE = CODE`로 작성돼 있어, `COMTCCMMNDETAILCODE` 테이블에 classic/quorum/stream 코드가 등록돼 있어야 목록 조회 정상 동작함
