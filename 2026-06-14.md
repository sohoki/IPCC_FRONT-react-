# 2026-06-14 작업 로그

---

## 09:00 — 백엔드 설정 파일 정리 (egovframework/com/config/)

**파일:** `backend/src/main/java/egovframework/com/config/`

### 변경 내용

총 20개 설정 파일 분석 후 불필요한 파일 삭제.

| 파일 | 조치 | 이유 |
|------|------|------|
| `EgovConfigAppTransaction.java` | **삭제** | Spring Boot 자동 트랜잭션 관리로 대체됨 |
| `EgovConfigAppWhitelist.java` | **삭제** | JSP URL 화이트리스트 — API 전용 백엔드에서 불필요 |
| `EgovConfigWebDispatcherServlet.java` | **삭제** | JSP/View 설정 — REST API 백엔드에 불필요 |
| `EgovWebServletContextListener.java` | **삭제** | 미사용 서블릿 컨텍스트 리스너 |
| `WebserverApplication.java` | **삭제** | 중복 `@SpringBootApplication` + 이미 `EgovBootApplication.java` 존재 |
| 나머지 15개 파일 | 유지 | 필수 설정으로 판단 |

`EgovConfigApp.java` 에서 `EgovConfigAppWhitelist.class` import 제거.

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `WebserverApplication.java` 삭제 후 부작용 연쇄 발생 (아래 항목들에서 해결)

---

## 09:30 — Hibernate 스키마 검증 오류 수정

**파일:** `backend/src/main/java/com/system/ipcc/pbx/avaya/models/SmsModelInfoEntity.java`

### 문제
```
SchemaManagementException: Schema-validation: wrong column type encountered
  column [SMS_USEYN] in table [SMS_MODEL_INFO]
  found [char (Types#CHAR)], but expecting [varchar(255) (Types#VARCHAR)]
```

- `WebserverApplication.java`가 살아있을 때는 `@Value("${server.servlet.port}")` 주입 실패로 시작 자체가 안 됐음
- 해당 파일 삭제 후 JPA 스키마 검증 단계까지 진행되면서 타입 불일치 노출

### 수정 내용
```java
// 수정 전
@Column(name = "SMS_USEYN")
private String smsUseyn;

// 수정 후
@Column(name = "SMS_USEYN", columnDefinition = "char(1)")
private String smsUseyn;
```

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- DB 컬럼이 `CHAR(1)` 타입인 경우 반드시 `columnDefinition = "char(1)"` 명시 필요

---

## 10:00 — HTTP 8081 포트 복구 (WebserverApplication.java 삭제 부작용)

**파일:** `backend/src/main/java/egovframework/com/config/HttpConnectorConfig.java` (신규 생성)

### 문제
- `WebserverApplication.java`가 `@Bean ServletWebServerFactory`로 HTTP 8081 추가 커넥터를 제공했음
- 파일 삭제 후 프론트엔드 → `http://localhost:8081` 호출 시 `ERR_CONNECTION_REFUSED`

### 수정 내용
`WebServerFactoryCustomizer` 방식으로 대체 (기존 Factory 교체 없이 커넥터만 추가):

```java
@Configuration
public class HttpConnectorConfig {

    @Value("${server.servlet.port}")
    private int httpPort;

    @Bean
    public WebServerFactoryCustomizer<TomcatServletWebServerFactory> httpConnectorCustomizer() {
        return factory -> {
            Connector connector = new Connector("org.apache.coyote.http11.Http11NioProtocol");
            connector.setPort(httpPort);
            factory.addAdditionalTomcatConnectors(connector);
        };
    }
}
```

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- `@Bean ServletWebServerFactory` 방식은 Spring Boot 기본 팩토리를 완전히 대체해 부작용 발생 가능
- `WebServerFactoryCustomizer` 방식이 올바른 접근법

---

## 10:30 — Tomcat TLD 스캔 경고 제거

**파일:** `backend/src/main/java/egovframework/com/config/TomcatScanConfig.java` (신규 생성)

### 문제
```
NoSuchFileException: ...xxx.jar (TLD 스캔 중 누락된 JAR 파일 참조)
```
- `build.gradle`의 `fileTree(dir: 'src/main/resources/libs', includes: ['*.jar'])` 설정
- IntelliJ 빌드 캐시가 삭제된 JAR 경로를 계속 참조
- API 전용 백엔드에서 TLD(Tag Library Descriptor) 스캔 자체가 불필요

### 수정 내용
```java
@Configuration
public class TomcatScanConfig {
    @Bean
    public WebServerFactoryCustomizer<TomcatServletWebServerFactory> disableTldScanning() {
        return factory -> factory.addContextCustomizers(context ->
            context.getJarScanner().setJarScanFilter((type, name) -> false)
        );
    }
}
```

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- JSP/TLD를 사용하지 않는 REST API 백엔드에서는 TLD 스캔을 비활성화하는 것이 정상

---

## 11:00 — CTI DB URL 오류 발견

**파일:** `EgovConfigAppDatasource.java` (AES 암호화 설정값)

### 문제
```
UnknownHostException: 1.255.226.51.33306
```
- AES 암호화된 CTI DB URL에 포트 구분자 오류
- 올바른 값: `jdbc:mysql://1.255.226.51:3306/...`
- 암호화된 값 내부: `.33306` (콜론 대신 점으로 인코딩됨)

### 이슈 / 개선사항
- 올바른 URL `jdbc:mysql://1.255.226.51:3306/DB_SETTLEMENT?...` 을 재암호화하여 설정값 교체 필요
- **미해결 상태** — 별도 작업 필요

---

## 11:30 — Integration PostgreSQL 접속 불가 (개발 환경)

### 문제
```
SocketTimeoutException: connect timed out (integration-pool)
```
- 개발 환경 네트워크에서 PostgreSQL 서버(integration DB)에 접근 불가
- HikariCP `minimumIdle` 기본값으로 인한 재연결 시도 WARN 로그 반복 출력

### 현재 상태
- **미해결** — 운영 환경 배포 시 자동 해결 예정
- 개발 환경에서는 WARN 무시

---

## 14:00 — JPA UPDATE 미작동 문제 (주요 이슈 — 3차 시도 끝에 해결)

**파일:**
- `backend/src/main/java/egovframework/com/config/EgovConfigAppDatasource.java`
- `backend/src/main/java/com/system/backoffice/bas/program/service/ProgrameInfoManageService.java`

### 문제
`updateProgrmInfo()` 에서 JPA `findById()` 후 엔티티 수정 시 UPDATE SQL이 발생하지 않음.
`SELECT`는 정상 실행되나 수정 내용이 DB에 반영되지 않음.

---

### 실패 이력 1 — DataSourceTransactionManager + dirty checking

```java
// 시도 1: DataSourceTransactionManager 트랜잭션 안에서 dirty checking 기대
@Transactional("transactionManager")
public int updateProgrmInfo(...) {
    ProgrmInfo existing = repository.findById(id).orElse(null);
    existing.updateProgrm(...);  // 필드 수정
    // save() 없이 dirty checking만 기대
    return 1;
}
```

**실패 원인:**
- `DataSourceTransactionManager`는 JDBC 커넥션만 관리
- JPA `EntityManager`의 생명주기를 인식하지 못함
- `findById()` 호출 시 EntityManager가 로컬 트랜잭션으로 별도 실행 → 커밋 후 EM 종료
- 반환된 엔티티는 **detached 상태** → dirty checking 불가

---

### 실패 이력 2 — DataSourceTransactionManager + save()

```java
@Transactional("transactionManager")
public int updateProgrmInfo(...) {
    ProgrmInfo existing = repository.findById(id).orElse(null);
    existing.updateProgrm(...);
    repository.save(existing);  // merge() 시도
    return 1;
}
```

**실패 원인:**
- `save(detachedEntity)` → 내부적으로 `em.merge()` 호출
- `DataSourceTransactionManager` 트랜잭션 안에서 `SharedEntityManagerCreator`는
  JPA 트랜잭션이 없으면 Transient EntityManager 생성
- Transient EM에서 `merge()` 실행 — UPDATE가 발생하지 않음

---

### 실패 이력 3 — JpaTransactionManager + setDataSource() + saveAndFlush()

```java
// EgovConfigAppDatasource.java 수정 시도
@Primary
@Bean(name = "transactionManager")
public PlatformTransactionManager transactionManager(EntityManagerFactory emf) {
    JpaTransactionManager txManager = new JpaTransactionManager(emf);
    txManager.setDataSource(dataSource());  // MyBatis 참여 목적
    return txManager;
}
```

```java
@Transactional("transactionManager")
public int updateProgrmInfo(...) {
    ProgrmInfo existing = repository.findById(id).orElse(null);
    existing.updateProgrm(...);
    repository.saveAndFlush(existing);  // 명시적 flush
    return 1;
}
```

**실패 원인:**
```
InvalidDataAccessApiUsageException: no transaction is in progress
  at SessionImpl.checkTransactionNeededForUpdateOperation
  at SessionImpl.doFlush
```

- `setDataSource()` 를 `JpaTransactionManager`에 설정하면
  Hibernate가 `DataSourceUtils`에서 JDBC 커넥션을 가져옴
- JPA 로컬 트랜잭션(`em.getTransaction()`)이 아닌 JDBC 트랜잭션으로 처리됨
- `flush()` 시점에 Hibernate가 JPA 트랜잭션 활성 여부 확인 → 없음 → 에러

---

### 해결책 — 트랜잭션 매니저 분리 운영

**핵심 아이디어:**
- `DataSourceTransactionManager` (기존) → MyBatis 전용으로 유지
- `JpaTransactionManager` (신규, `setDataSource()` 없음) → JPA write 전용

```java
// EgovConfigAppDatasource.java 추가
@Bean(name = "jpaTransactionManager")
public PlatformTransactionManager jpaTransactionManager(EntityManagerFactory emf) {
    return new JpaTransactionManager(emf);  // setDataSource() 호출 없음
}
```

```java
// ProgrameInfoManageService.java
@Transactional("jpaTransactionManager")  // ← 변경 포인트
public int updateProgrmInfo(ProgrmInfoDto progrmInfoDto) throws Exception {
    try {
        if (Globals.SAVE_MODE_INSERT.equals(progrmInfoDto.getMode())) {
            ProgrmInfo newEntity = ProgrmInfo.builder()
                    .progrmFileNm(progrmInfoDto.getProgrmFileNm())
                    .progrmStrePath(progrmInfoDto.getProgrmStrePath())
                    .progrmKoreanNm(progrmInfoDto.getProgrmKoreannm())
                    .progrmDc(progrmInfoDto.getProgrmDc())
                    .url(progrmInfoDto.getUrl())
                    .build();
            repository.save(newEntity);
        } else {
            ProgrmInfo existing = repository.findById(progrmInfoDto.getProgrmFileNm())
                    .orElseThrow(() -> new IllegalArgumentException(
                        "수정할 프로그램 정보가 없습니다: " + progrmInfoDto.getProgrmFileNm()));
            existing.updateProgrm(
                    progrmInfoDto.getProgrmStrePath(),
                    progrmInfoDto.getProgrmKoreannm(),
                    progrmInfoDto.getProgrmDc(),
                    progrmInfoDto.getUrl()
            );
            // save() 없음 — JpaTransactionManager 커밋 시 dirty checking이 UPDATE 자동 발생
        }
        return 1;
    } catch (Exception e) {
        log.error("updateProgrmInfo error", e);
        return -1;
    }
}
```

### 동작 원리 (성공)

```
jpaTransactionManager 트랜잭션 시작
    ↓
EntityManagerHolder를 현재 스레드에 바인딩
    ↓
findById() → SharedEntityManager가 바인딩된 EM 사용 → managed entity 반환
    ↓
existing.updateProgrm() → managed entity 필드 변경
    ↓
메서드 리턴
    ↓
JpaTransactionManager.commit()
    ↓
Hibernate dirty checking → 변경된 필드 감지
    ↓
UPDATE SQL 자동 실행
```

### 리뷰 체크리스트 결과
- [x] Code quality & readability
- [x] Naming conventions & style
- [x] Logic errors & edge cases
- [x] Security vulnerabilities
- [x] Performance concerns
- [ ] Test coverage

### 이슈 / 개선사항
- JPA write가 필요한 다른 서비스 메서드도 동일하게 `@Transactional("jpaTransactionManager")` 적용 필요
- MyBatis와 JPA를 같은 트랜잭션 안에서 함께 써야 하는 경우 별도 설계 필요

---

## 오늘 작업 전체 요약

| 항목 | 상태 |
|------|------|
| 설정 파일 5개 삭제 (불필요 파일 정리) | ✅ 완료 |
| Hibernate `char` vs `varchar(255)` 스키마 오류 | ✅ 완료 |
| HTTP 8081 포트 복구 (HttpConnectorConfig.java) | ✅ 완료 |
| Tomcat TLD 스캔 경고 제거 (TomcatScanConfig.java) | ✅ 완료 |
| JPA UPDATE dirty checking 작동 (3차 시도 성공) | ✅ 완료 |
| CTI DB URL `.33306` 오류 (재암호화 필요) | ⏳ 미완료 |
| Integration PostgreSQL 개발 환경 접속 불가 | ⏳ 미완료 (운영 배포 시 자동 해결) |

---

## 핵심 교훈 — JPA + MyBatis 혼용 프로젝트

| 상황 | 사용할 트랜잭션 매니저 |
|------|----------------------|
| MyBatis INSERT/UPDATE/DELETE | `@Transactional("transactionManager")` |
| JPA `findById()` (단순 조회) | 트랜잭션 불필요 또는 `@Transactional(readOnly=true)` |
| JPA dirty checking UPDATE | `@Transactional("jpaTransactionManager")` |
| JPA `deleteById()` | 별도 `@Transactional` 또는 `jpaTransactionManager` |

`JpaTransactionManager`에 `setDataSource()`를 설정하면 Hibernate 커넥션 관리와 충돌하므로 **사용하지 않는다**.
